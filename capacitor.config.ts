import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.shopApp',
  appName: 'shopApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
